import React, { Component } from 'react';
import { Text } from 'react-native';

export default class UnitQuickSelectors extends Component {
  render() {
    return (
      <Item>
        <Text>wow i wonder when this will expire</Text>
      </Item>
    );
  }
}
